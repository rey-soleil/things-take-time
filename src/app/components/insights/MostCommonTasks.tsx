import { ClusteredTasks } from "@/app/utils";

type MostCommonTasksProps = {
  clusteredTasks: ClusteredTasks;
};

export default function MostCommonTasks({
  clusteredTasks,
}: MostCommonTasksProps) {
  return <div></div>;
}
